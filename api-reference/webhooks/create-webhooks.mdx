---
title: "POST /v1/webhooks"
api: "POST /v1/webhooks"
description: "GET /api/v1/webhooks List webhook subscriptions for the team"
---
# POST /v1/webhooks
GET /api/v1/webhooks List webhook subscriptions for the team

## Request Body



## Responses

<ResponseField name="200" type="success">
  Successful response
</ResponseField>

<ResponseField name="400" type="error">
  Bad request - Invalid parameters

  Returns a [`Error`](#components/schemas/Error) object
</ResponseField>

<ResponseField name="401" type="error">
  Unauthorized - Invalid or missing API key

  Returns a [`Error`](#components/schemas/Error) object
</ResponseField>

<ResponseField name="404" type="error">
  Not found

  Returns a [`Error`](#components/schemas/Error) object
</ResponseField>

<ResponseField name="500" type="error">
  Internal server error

  Returns a [`Error`](#components/schemas/Error) object
</ResponseField>



## Code Examples

<CodeGroup>

```bash cURL
curl -X POST https://www.skaala.ai/api/v1/webhooks \
  -H "X-API-Key: sk_live_your_key_here" \
  -H "Content-Type: application/json" \
  -d '{
    // Your request body here
  }'
```

```javascript JavaScript
const response = await fetch('https://www.skaala.ai/api/v1/webhooks', {
  method: 'POST',
  headers: {
    'X-API-Key': process.env.SKAALA_API_KEY,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    // Your request body here
  })
});

const data = await response.json();
console.log(data);
```

```python Python
import requests
import os

response = requests.post(
    'https://www.skaala.ai/api/v1/webhooks',
    headers={'X-API-Key': os.environ['SKAALA_API_KEY']},
    json={
        # Your request body here
    }
)

data = response.json()
print(data)
```

</CodeGroup>
