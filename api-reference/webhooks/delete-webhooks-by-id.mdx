---
title: "DELETE /v1/webhooks/{webhookId}"
api: "DELETE /v1/webhooks/{webhookId}"
description: "GET /api/v1/webhooks/[webhookId] Get a single webhook subscription"
---
# DELETE /v1/webhooks/{webhookId}
GET /api/v1/webhooks/[webhookId] Get a single webhook subscription

## Path Parameters

<ParamField path="webhookId" type="string" optional>
  webhookId
</ParamField>


## Responses

<ResponseField name="200" type="success">
  Successful response
</ResponseField>

<ResponseField name="400" type="error">
  Bad request - Invalid parameters

  Returns a [`Error`](#components/schemas/Error) object
</ResponseField>

<ResponseField name="401" type="error">
  Unauthorized - Invalid or missing API key

  Returns a [`Error`](#components/schemas/Error) object
</ResponseField>

<ResponseField name="404" type="error">
  Not found

  Returns a [`Error`](#components/schemas/Error) object
</ResponseField>

<ResponseField name="500" type="error">
  Internal server error

  Returns a [`Error`](#components/schemas/Error) object
</ResponseField>



## Code Examples

<CodeGroup>

```bash cURL
curl -X DELETE https://www.skaala.ai/api/v1/webhooks/example_webhookId \
  -H "X-API-Key: sk_live_your_key_here"
```

```javascript JavaScript
const response = await fetch('https://www.skaala.ai/api/v1/webhooks/example_webhookId', {
  method: 'DELETE',
  headers: {
    'X-API-Key': process.env.SKAALA_API_KEY
  }
});

const data = await response.json();
console.log(data);
```

```python Python
import requests
import os

response = requests.delete(
    'https://www.skaala.ai/api/v1/webhooks/example_webhookId',
    headers={'X-API-Key': os.environ['SKAALA_API_KEY']}
)

data = response.json()
print(data)
```

</CodeGroup>
